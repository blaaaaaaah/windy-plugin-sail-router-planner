---
phase: 01-technical-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/types/route.ts, src/types/weather.ts, src/types/forecast.ts]
autonomous: true
requirements: [WEATHER-01, WEATHER-02]

must_haves:
  truths:
    - "RouteDefinition can be created with start point and departure time"
    - "Route legs can be added with speed and endpoint calculations"
    - "WeatherForecast contains all required sailing data fields"
  artifacts:
    - path: "src/types/route.ts"
      provides: "RouteDefinition and RouteLeg classes with sailing methods"
      exports: ["RouteDefinition", "RouteLeg"]
    - path: "src/types/weather.ts"
      provides: "Weather data structures matching Windy API"
      exports: ["WeatherForecast", "PointForecast"]
    - path: "src/types/forecast.ts"
      provides: "Integration types for weather service"
      exports: ["WeatherForecastService", "RouteForecast"]
  key_links:
    - from: "src/types/route.ts"
      to: "src/types/forecast.ts"
      via: "RouteDefinition used in RouteForecast"
      pattern: "RouteForecast.*RouteDefinition"
---

<objective>
Create foundational data structures for sailing route planning and weather forecasting that match the user's specifications and enable type-safe development.

Purpose: Establish the core type system that all subsequent weather and calculation functionality will build upon
Output: TypeScript interfaces and classes for routes, weather data, and forecast integration
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-technical-foundation/01-CONTEXT.md
@.planning/phases/01-technical-foundation/01-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create core route data structures</name>
  <files>src/types/route.ts, test/types/route.test.ts</files>
  <behavior>
    - RouteDefinition creates with startPoint (LatLn) and departureTime (timestamp)
    - addLeg(endPoint, averageSpeedKnots) adds RouteLeg to internal array
    - getLegs() returns array of RouteLeg objects
    - RouteLeg contains: startTime, startPoint, endPoint, course (-180 to 180), distance, averageSpeed, endTime
    - Course calculation handles proper bearing between lat/lng points
    - Distance calculation returns nautical miles
    - Time calculation accounts for speed to determine arrival
  </behavior>
  <action>
    Create RouteDefinition and RouteLeg classes per user specification. RouteDefinition holds LatLn startPoint, departureTime timestamp, and internal RouteLeg array. Implement addLeg() method that calculates course (bearing -180 to 180), distance in nautical miles, and endTime based on averageSpeedKnots. RouteLeg is value object containing all specified fields. Use precise nautical calculations for course/distance - this is critical for sailing accuracy.
  </action>
  <verify>
    <automated>npm test -- test/types/route.test.ts</automated>
  </verify>
  <done>RouteDefinition and RouteLeg classes implemented with working addLeg() method and proper nautical calculations</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create weather data structures</name>
  <files>src/types/weather.ts, test/types/weather.test.ts</files>
  <behavior>
    - WeatherForecast contains windSpeed/Direction, gustsSpeed/Direction, currentSpeed/Direction, wavesHeight/Period/Direction
    - Direction fields use 0-359 north up for northUp weather, -180-180 apparent for apparent weather
    - All speed fields in knots, wave height in meters
    - PointForecast contains interpolated LatLn point, timestamp, leg reference, warnings array, northUp WeatherForecast, apparent WeatherForecast, precipitations, weather
    - Structure matches expected Windy API response format for integration
  </behavior>
  <action>
    Create WeatherForecast interface with all sailing weather fields per user specification. Include windSpeed/Direction (0-359 north up), gustsSpeed/Direction, currentSpeed/Direction, wavesHeight/Period/Direction. Create PointForecast that contains interpolated position, timestamp, leg reference, warnings, and both northUp and apparent WeatherForecast objects. Structure must match Windy's API response format discovered in research - validate field names and types against expected API.
  </action>
  <verify>
    <automated>npm test -- test/types/weather.test.ts</automated>
  </verify>
  <done>Weather data structures created matching Windy API format with proper direction/speed field conventions</done>
</task>

<task type="auto">
  <name>Task 3: Create forecast service integration types</name>
  <files>src/types/forecast.ts</files>
  <action>
    Create WeatherForecastService interface that takes WindyAPI parameter and provides getRouteForecast(route: RouteDefinition): RouteForecast method. Create RouteForecast interface containing route reference and pointForecasts array of PointForecast objects. These are the contracts that the weather integration and calculations will implement against. Keep interfaces focused on data shape, implementation comes later.
  </action>
  <verify>
    <automated>tsc --noEmit src/types/forecast.ts</automated>
  </verify>
  <done>Service interfaces created defining contracts for weather forecasting integration</done>
</task>

</tasks>

<verification>
All core data structures exist as TypeScript files and pass compilation. Classes and interfaces match user specifications exactly. Tests validate critical sailing calculations (course, distance, time). Type system enables type-safe development for weather integration.
</verification>

<success_criteria>
- RouteDefinition class works with addLeg() creating proper RouteLeg objects
- Weather structures include all required sailing fields with proper direction conventions
- Forecast service types provide clear contracts for later implementation
- All code follows TypeScript strict mode and compiles without errors
- Test coverage validates sailing calculation accuracy
</success_criteria>

<output>
After completion, create `.planning/phases/01-technical-foundation/01-01-SUMMARY.md`
</output>