---
phase: 01-technical-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [.planning/phases/01-technical-foundation/01-01-PLAN.md, test/types/RouteTypes.test.ts, test/types/WeatherTypes.test.ts]
autonomous: true
requirements: [WEATHER-01, WEATHER-02]
gap_closure: true

must_haves:
  truths:
    - "Plan 01-01 frontmatter accurately reflects actual implementation file paths"
    - "Test coverage validates RouteDefinition sailing calculations work correctly"
    - "Test coverage validates WeatherTypes match Windy API structure requirements"
  artifacts:
    - path: ".planning/phases/01-technical-foundation/01-01-PLAN.md"
      provides: "Corrected frontmatter with actual file paths"
      contains: "files_modified: [src/types/RouteTypes.ts, src/types/WeatherTypes.ts]"
    - path: "test/types/RouteTypes.test.ts"
      provides: "Test coverage for RouteDefinition class"
      min_lines: 40
      exports: ["describe", "test"]
    - path: "test/types/WeatherTypes.test.ts"
      provides: "Test coverage for weather data structures"
      min_lines: 30
      exports: ["describe", "test"]
  key_links:
    - from: "test/types/RouteTypes.test.ts"
      to: "src/types/RouteTypes.ts"
      via: "import statements and test coverage"
      pattern: "import.*RouteTypes"
    - from: "test/types/WeatherTypes.test.ts"
      to: "src/types/WeatherTypes.ts"
      via: "import statements and test coverage"
      pattern: "import.*WeatherTypes"
---

<objective>
Close verification gaps from Phase 1 Plan 1 by correcting plan documentation and adding missing test coverage.

Purpose: Ensure plan documentation matches actual implementation and provide test coverage that validates sailing calculations work correctly
Output: Updated plan frontmatter and comprehensive test suite for core types
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-technical-foundation/01-VERIFICATION.md
@.planning/phases/01-technical-foundation/01-01-SUMMARY.md
@src/types/RouteTypes.ts
@src/types/WeatherTypes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct Plan 01-01 frontmatter file paths</name>
  <files>.planning/phases/01-technical-foundation/01-01-PLAN.md</files>
  <action>
    Update the frontmatter `files_modified` field to reflect the actual implementation file paths. Change from [src/types/route.ts, src/types/weather.ts, src/types/forecast.ts] to [src/types/RouteTypes.ts, src/types/WeatherTypes.ts, src/types/Coordinates.ts, src/utils/NavigationUtils.ts, src/services/WeatherForecastService.ts, package.json]. Also update the must_haves artifacts section to reference the actual file paths that exist. This addresses the critical documentation gap identified in verification where plan specified wrong file paths but implementation exists with different names.
  </action>
  <verify>grep "src/types/RouteTypes.ts" .planning/phases/01-technical-foundation/01-01-PLAN.md</verify>
  <done>Plan 01-01 frontmatter accurately reflects the actual implementation file paths and structure</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RouteDefinition test coverage</name>
  <files>test/types/RouteTypes.test.ts</files>
  <behavior>
    - RouteDefinition creates with startPoint (LatLng) and departureTime (timestamp)
    - addLeg(endPoint, averageSpeedKnots) adds RouteLeg with proper calculations
    - Course calculation returns bearing between -180 and 180 degrees
    - Distance calculation returns accurate nautical miles using spherical trigonometry
    - Time calculation correctly accounts for speed to determine arrival time
    - Edge cases: same start/end point, very short distances, crossing date line
  </behavior>
  <action>
    Create comprehensive test suite for RouteDefinition class that validates the sailing calculations work correctly. Test route creation with real coordinates, addLeg functionality with proper course/distance/time calculations, and edge cases like same start/end points. Use known coordinate pairs to validate mathematical accuracy - test from research shows 0°N 0°W to 1°N 1°E should be ~45° course. Include tests for the RouteLeg structure and all calculated fields. Ensure tests use actual Leaflet LatLng objects as required by the implementation.
  </action>
  <verify>
    <automated>npm test -- test/types/RouteTypes.test.ts</automated>
  </verify>
  <done>RouteDefinition class has comprehensive test coverage validating sailing calculation accuracy</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create WeatherTypes test coverage</name>
  <files>test/types/WeatherTypes.test.ts</files>
  <behavior>
    - WeatherData interface contains all required sailing fields (wind, gusts, waves, current)
    - Direction fields properly handle 0-359 north up and -180-180 apparent conventions
    - RouteForecast structure matches expected service response format
    - Type safety ensures no undefined/null values in critical weather fields
    - Weather data transformation preserves sailing-critical information
  </behavior>
  <action>
    Create test suite for weather data structures that validates the types match Windy API requirements and sailing needs. Test WeatherData interface completeness, direction field conventions (north up vs apparent), and RouteForecast structure. Include tests that verify the weather data transformation from API format to sailing-specific apparent wind calculations preserves all critical information. Validate that all required fields from WEATHER-01 and WEATHER-02 requirements are properly typed and accessible.
  </action>
  <verify>
    <automated>npm test -- test/types/WeatherTypes.test.ts</automated>
  </verify>
  <done>WeatherTypes have comprehensive test coverage validating API integration and sailing data requirements</done>
</task>

</tasks>

<verification>
All verification gaps closed: Plan documentation matches actual implementation, test coverage validates sailing calculations work correctly, and weather types meet API integration requirements.
</verification>

<success_criteria>
- Plan 01-01 frontmatter reflects actual file paths created during implementation
- RouteDefinition class has test coverage proving sailing calculations are mathematically accurate
- WeatherTypes have test coverage validating API structure compatibility
- All tests pass and provide confidence in the technical foundation
</success_criteria>

<output>
After completion, create `.planning/phases/01-technical-foundation/01-02-SUMMARY.md`
</output>